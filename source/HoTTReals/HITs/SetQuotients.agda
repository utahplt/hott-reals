-- Mirrors Cubical.HITs.SetQuotients, but re-exports our vendored copy of the
-- eliminators. The HIT itself is unchanged upstream, so Base is taken as is.
module HoTTReals.HITs.SetQuotients where

open import Cubical.HITs.SetQuotients.Base public
open import HoTTReals.HITs.SetQuotients.Properties public
